{
  "version": 2,
  "builds": [
    {
      "src": "main.ts",
      "use": "@vercel/frameworks",
      "config": { "runtime": "deno" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "main.ts" }
  ]
}
